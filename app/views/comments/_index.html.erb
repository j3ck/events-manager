<ul class="comments">
	<%= render :partial => "comments/comment", :collection => current_user.comments.order("created_at ASC"), :as => 'comment' %>
</ul>
<%= render "comments/form", :comment => current_user.comments.build(:event_id => @event.id) %>