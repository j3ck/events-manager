<%= render "comments/form", :comment => current_user.comments.build(:event_id => @event.id) %>
<ul class="media-list comments">
	<%= render :partial => "comments/comment", :collection => @event.comments.order("created_at ASC"), :as => 'comment' %>
</ul>